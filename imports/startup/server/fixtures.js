/* Any operations that should be performed when application is started should be done here.
This file can be used to populate database with initial data */

import { Meteor } from 'meteor/meteor';

Meteor.startup(() => {
  /* Populating database with initial sampleCollections

  if (SampleCollections.find().count() === 0) {
    for(let i = 0; i < 10; i = i + 1) {
      SampleCollections.insert({
        names: `collection ${i}`
      });
    }
  }

  */
});
